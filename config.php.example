<?php
// config.php.example – Vorlage für die Konfigurationsdatei
// WICHTIG: config.php liegt im Projektverzeichnis, NICHT in public/
// Sie wird niemals ins Repository eingecheckt.
//
// Verwendung:
//   cp config.php.example config.php
//   Dann Werte anpassen.
//
// Einbindung in lib/db.php und lib/auth.php:
//   $config = require dirname(__DIR__) . '/config.php';
//   (von lib/ aus: eine Ebene hoch zum Projektverzeichnis)

return [
    // Datenbankverbindung
    'db_host' => 'localhost',
    'db_name' => 'marego_kurse',
    'db_user' => 'marego_user',
    'db_pass' => 'HIER_PASSWORT_EINTRAGEN',

    // Admin-Passwort als bcrypt-Hash
    // Erzeugen mit: php -r "echo password_hash('meinPasswort', PASSWORD_BCRYPT);"
    'admin_hash' => 'HIER_BCRYPT_HASH_EINTRAGEN',

    // HAFAS-API (INSA/NASA) – nur ändern wenn sich der Endpunkt ändert
    'hafas_url'    => 'https://reiseauskunft.insa.de/bin/mgate.exe',
    'hafas_aid'    => 'nasa-apps',
    'hafas_client' => ['type' => 'IPH', 'id' => 'NASA', 'v' => '4000200', 'name' => 'nasaPROD'],
    'hafas_ver'    => '1.44',

    // Tabellen-Prefix (optional; Standard: kein Prefix).
    // Erlaubt mehrere Instanzen auf einer Datenbank.
    // Beispiel: 'db_prefix' => 'trammd_'  →  Tabellen: trammd_trips, trammd_recordings, …
    // 'db_prefix' => '',

    // HAFAS-Logging: echte API-Aufrufe in hafas_log-Tabelle protokollieren (optional; Standard: aus).
    // Tabelle muss via DATABASE.sql angelegt sein. Lazy-Cleanup alle 7 Tage, 2 % Wahrscheinlichkeit.
    // 'hafas_logging' => true,

    // Haltestellenpräfix, der in der Anzeige entfernt wird (optional; Standard: kein Präfix).
    // Beispiel: 'stop_name_prefix' => 'Magdeburg, '  →  "Magdeburg, ZOB/Adelheidring" → "ZOB/Adelheidring"
    // 'stop_name_prefix' => 'Magdeburg, ',

    // Aktive Region für gesetzliche Feiertage (zweistelliger Bundesland-Code, optional; Standard: 'ST').
    // Die Feiertagsliste kommt aus public_holidays.json, gefiltert auf diese Region.
    // Erlaubte Werte: BW, BY, BE, BB, HB, HH, HE, MV, NI, NW, RP, SL, SN, ST, SH, TH
    // 'active_region' => 'ST',

    // HAFAS-Cache-Verzeichnis (optional; Standard: <projektroot>/cache/hafas/)
    // Muss außerhalb des Webroots liegen und für den Webserver-Prozess schreibbar sein.
    // 'cache_dir' => '/var/cache/mdkurstracker/hafas',
];
